# 项目构建打包

接下来我们对项目进行构建打包，并进行产物的预览。

## 1. 构建打包

在项目根目录下执行以下命令：

```bash
npm run build
```

执行完毕后，会在 `.island` 目录下生成一个 dist 目录，里面就是我们的打包产物。

## 2. 本地预览

在项目根目录下执行以下命令：

```bash
npm run preview
```

执行完毕后，终端会显示一个本地预览地址，在浏览器打开即可看到预览页面。
